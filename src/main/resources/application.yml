spring:
  application:
    name: chat-app

  datasource:
    url: jdbc:mysql://localhost:3306/chatapp?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh
    username: root
    password: 12345678

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080